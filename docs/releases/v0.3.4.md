# AgentDir v0.3.4

AgentDir v0.3.4 updates the project introduction for clearer presentation and first-read comprehension.

## What Changed

- Adds a top-level "What is AgentDir" README section
- Explains AgentDir as a local-first flight recorder and memory layer for coding agents
- Summarizes automatic session memory, evidence-backed coding sessions, replay, local storage, Maildir durability, searchable memory, handoffs, and agent-owned workflow
- Keeps install snippets and release assets aligned with the current patch release

## Install

```bash
gh api -H "Accept: application/vnd.github.raw" \
  'repos/jstxn/agentdir/contents/scripts/install.sh?ref=v0.3.4' | bash
```
